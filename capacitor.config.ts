import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'movies-app-virginia',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
